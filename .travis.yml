language: ruby
rvm: 2.6.3
cache:
  - bundler
env:
  - COMPOSE_VERSION: 1.24.1
  - RAILS_MASTER_KEY=hellworld
  - RACK_ENV=test

script:
  - bundle install
  - bundle exec rake spec

# notifications:
#   email:
#     recipients:
#     - thapakazi@dibya.org
#     on_success: always
#     on_failure: always
